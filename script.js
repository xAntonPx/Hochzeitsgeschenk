const cfg=window.SITE_CONFIG,$=s=>document.querySelector(s),preview=new URLSearchParams(location.search).get('preview')==='1';$('#name').textContent=cfg.name;$('#countdownLine').textContent=cfg.countdownLine;$('#music').src=cfg.music;cfg.photos.forEach(src=>{const i=document.createElement('img');i.src=src;i.alt='Unser schöner Moment';$('#gallery').appendChild(i)});const target=new Date(cfg.unlockAt).getTime();let timer;function tick(){const diff=target-Date.now();if(preview||diff<=0){clearInterval(timer);$('#countdownPage').classList.add('hidden');$('#unlockPage').classList.remove('hidden');return}$('#d').textContent=String(Math.floor(diff/86400000)).padStart(3,'0');$('#h').textContent=String(Math.floor(diff/3600000)%24).padStart(2,'0');$('#m').textContent=String(Math.floor(diff/60000)%60).padStart(2,'0');$('#s').textContent=String(Math.floor(diff/1000)%60).padStart(2,'0')}timer=setInterval(tick,1000);tick();$('#openBtn').onclick=async()=>{$('#unlockPage').classList.add('hidden');$('#story').classList.remove('hidden');try{await $('#music').play()}catch(e){}window.scrollTo(0,0);observe()};$('#suitcase').onclick=()=>{$('#suitcase').classList.add('open');setTimeout(()=>$('#gift').classList.remove('hidden'),450)};function observe(){const o=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('visible')),{threshold:.18});document.querySelectorAll('.reveal').forEach(el=>o.observe(el))}observe();const c=$('#stars'),x=c.getContext('2d');let p=[];function resize(){c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);p=Array.from({length:90},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.4+.2,a:Math.random()}))}function draw(){x.clearRect(0,0,innerWidth,innerHeight);p.forEach(q=>{q.a+=.02;x.globalAlpha=.35+.65*Math.abs(Math.sin(q.a));x.fillStyle='#ffd36f';x.beginPath();x.arc(q.x,q.y,q.r,0,Math.PI*2);x.fill()});requestAnimationFrame(draw)}addEventListener('resize',resize);resize();draw();